import React from "react";

// ? NOT USED at the moment
// External Components
import { Box } from "@gedesurya125/surya-ui";
export const RightSideBar = () => {
  return (
    <Box
      as="aside"
      sx={{
        width: [null, "4rem"],
        bg: "teal",
      }}
    >
      Hello from side bar right
    </Box>
  );
};
