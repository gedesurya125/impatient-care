import React from "react";

// Local Components
import { Box } from "@gedesurya125/surya-ui";

export const NavigationBar = () => {
  return (
    <Box
      as="nav"
      sx={{
        gridColumn: ["null", "1/ span2"],
      }}
    >
      NavigationBar
    </Box>
  );
};
